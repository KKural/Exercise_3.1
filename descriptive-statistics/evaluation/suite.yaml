- tab: "Multiple Choice"
  testcases:
    - description: "Answer must be the single digit 2"
      expression: "stdout.strip()"   # <-- what we evaluate
      return: "2"                    # <-- what it must equal
