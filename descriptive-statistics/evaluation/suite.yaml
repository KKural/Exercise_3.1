#  ── TESTed “playlist” DSL (current production)

- unit: answer

  scripts:
    run-wrapper:
      cmd: ["python3", "wrapper.py", "{{ STUDENT_FILE }}"]

  tabs:
    - name: Answer
      testcases:
        - script: run-wrapper
          stdout: "2\n"
          description: "✅ Correct – Descriptive statistics summarise and describe data."

        - script: run-wrapper
          stdout: "1\n"
          description: "❌ Predicting is done with inferential statistics."

        - script: run-wrapper
          stdout: "3\n"
          description: "❌ Causal relations belong to inferential analysis."
