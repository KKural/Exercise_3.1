
---

### 4  evaluation/suite.yaml   *(DSL v3 – 100 % validated)*

```yaml
unit: answer                       # logical name – free to choose

scripts:
  read-answer:
    cmd: ["python3", "{{ STUDENT_FILE }}"]

tabs:
  - name: Answer
    contexts:
      - cases:
          - description: |
              ✅ Correct – Descriptive statistics summarise and describe data.
            script: read-answer
            stdout: "^\\s*2\\s*$"   # ↖ anchored regex, ONLY 2 is accepted
            stdout_regex: true
